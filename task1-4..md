# task1-task4 主要内容总结

## task1：RPA 版本（起点）
- 目标：完成佛山历史拍地数据的初版自动化采集。
- 方式：基于 RPA 工具流程实现页面操作与数据导出。
- 交付：以视频和 Excel 结果为主，体现“能跑通流程”的阶段成果。
- 特点：开发门槛较低、上手快，但可维护性和可扩展性有限。

## task2：Python 版本（工程化采集）
- 目标：用 Python 重构采集流程，提升稳定性和可控性。
- 路线 1（Selenium）：
	- 自动点击历史交易页、翻页、进入详情页。
	- 处理多窗口和 iframe 后提取字段并写入 Excel。
- 路线 2（API 逆向）：
	- 直接请求接口获取列表数据。
	- 对详情页补充字段（如土地面积），并加入重试与超时控制。
- 特点：相比 task1 更工程化，且验证了“API 逆向”在效率和准确性上的优势。

## task3：多城市 + OCR/LLM 增强（复杂场景突破）
- 目标：从单城市扩展到多城市，并解决复杂网页与非结构化内容提取问题。
- 核心能力：
	- 多城市配置驱动（通过城市配置文件切换 XPath）。
	- Python OCR 路线：截图 + Tesseract + 正则提取。
	- 大模型路线：截图后调用 Qwen 做语义提取并结构化输出。
	- 补充 RPA 路线用于对照实验。
- 背景问题：部分页面存在 Shadow DOM 限制，传统 Selenium 定位困难。
- 特点：实现了“从网页非结构化文本到结构化字段”的关键升级。

## task4：实时监控与邮件通知（闭环应用）
- 目标：把采集能力升级为实时监测与告警能力。
- 实现流程：
	- 定时轮询（每 15 秒）检查目标页面序列号。
	- 比较新旧序列号，判断是否发生更新。
	- 通过 SMTP 自动发送邮件通知监测结果。
- 特点：从“离线采集”走向“在线监控 + 主动通知”，形成业务闭环。

## 总体演进脉络
- task1：先跑通（RPA）。
- task2：再工程化（Python + Selenium/API）。
- task3：攻克复杂页面与语义提取（OCR/LLM + 多城市）。
- task4：落到实时监控与告警（可持续运行场景）。

